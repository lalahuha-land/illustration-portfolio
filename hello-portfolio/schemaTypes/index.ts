import {illustration} from '../schemas/illustration'
import {about} from '../schemas/about'
import {siteSettings} from '../schemas/siteSettings'

export const schemaTypes = [illustration, about, siteSettings]
